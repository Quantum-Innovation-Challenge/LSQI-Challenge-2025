# Core numerics & data
numpy>=1.24
pandas>=1.5
scikit-learn>=1.2
joblib>=1.2

# Visualization & progress
matplotlib>=3.6
tqdm>=4.64

# PyTorch (CPU by default; install CUDA build from pytorch.org if needed)
torch>=2.2

# PennyLane (quantum differentiable programming)
pennylane>=0.35
# Optional: extra quantum chemistry plugins (not required)
# pennylane-qchem>=0.35
